$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805987
src/com/levelonelabs/aimbot/modules/BabelModule.java 110 124
src/com/levelonelabs/aimbot/modules/StockModule.java 150 164
----------------------------------------
            URL url=new URL(query);

            //setup the connection (do POST)
            URLConnection connection=url.openConnection();
            connection.setDoOutput(true);
            connection.connect();

            //read the results page
            BufferedReader br=new BufferedReader(new InputStreamReader(connection.getInputStream()));
            String line;
            StringBuffer sb=new StringBuffer();
            while((line=br.readLine()) != null) {
                sb.append(line+"\n");
            }
            br.close();

----------------------------------------
                    URL url= new URL(queryURL);

                    //setup the connection (do POST)
                    URLConnection connection= url.openConnection();
                    connection.setDoOutput(true);
                    connection.connect();

                    //read the results page
                    BufferedReader br= new BufferedReader(new InputStreamReader(connection.getInputStream()));
                    String line;
                    StringBuffer sb= new StringBuffer();
                    while ((line= br.readLine()) != null) {
                        sb.append(line + "\n");
                    }
                    br.close();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805988
src/com/levelonelabs/aimbot/modules/ReminderModule.java 243 249
src/com/levelonelabs/aimbot/modules/ScheduleModule.java 427 433
----------------------------------------
        ArrayList br = (ArrayList) reminders.get(buddy.getName());
        if ((br != null) && (i >= 0) && (i < br.size())) {
            br.remove(i);
            return true;
        }
        return false;
    }

----------------------------------------
        ArrayList br = (ArrayList) events.get(buddy.getName());
        if ((br != null) && (i >= 0) && (i < br.size())) {
            br.remove(i);
            return true;
        }
        return false;
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805989
src/com/levelonelabs/aimbot/modules/ReminderModule.java 287 298
src/com/levelonelabs/aimbot/modules/ScheduleModule.java 521 532
----------------------------------------
    public void writeState(Element emptyStateElement) {
        Document doc = emptyStateElement.getOwnerDocument();
        Iterator iter = reminders.keySet().iterator();

        while (iter.hasNext()) {
            String name = (String) iter.next();
            ArrayList br = (ArrayList) reminders.get(name);
            if (br.size() > 0) {
                Element buddyElem = doc.createElement("buddy");
                buddyElem.setAttribute("name", name);
                for (int i = 0; i < br.size(); i++) {
                    Element remElem = doc.createElement("reminder");

----------------------------------------
    public void writeState(Element emptyStateElement) {
        Document doc = emptyStateElement.getOwnerDocument();
        Iterator iter = events.keySet().iterator();

        while (iter.hasNext()) {
            String name = (String) iter.next();
            ArrayList br = (ArrayList) events.get(name);
            if (br.size() > 0) {
                Element buddyElem = doc.createElement("buddy");
                buddyElem.setAttribute("name", name);
                for (int i = 0; i < br.size(); i++) {
                    Element remElem = doc.createElement("event");

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805990
src/com/levelonelabs/aimbot/modules/UserInformationModule.java 173 185
src/com/levelonelabs/aimbot/modules/UserInformationModule.java 209 221
----------------------------------------
				sendMessage(buddy, result);
				return;
			}
		} else if (query.toLowerCase().trim().equals("users")) {
			String result = "";
			int num = 0;

			for (Iterator iter = getBuddyNames(); iter.hasNext();) {
				String name = (String) iter.next();
				AIMBuddy bud = getBuddy(name);
				if (bud == null) {
					continue;
				}

----------------------------------------
				sendMessage(buddy, "Current (<b>" + num + "</b>) Users are:\n" + result);
				return;
			}
		} else if (query.toLowerCase().trim().equals("enemies")) {
			String result = "";
			int num = 0;

			for (Iterator iter = getBuddyNames(); iter.hasNext();) {
				String name = (String) iter.next();
				AIMBuddy bud = getBuddy(name);
				if (bud == null) {
					continue;
				}

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805991
src/com/levelonelabs/aimbot/modules/TVListingsModule.java 135 149
src/com/levelonelabs/aimbot/modules/WeatherModule.java 120 134
----------------------------------------
	public void performService(AIMBuddy buddy, String query) {
		if (query.trim().toLowerCase().startsWith("tvlist")) {
			StringTokenizer st = new StringTokenizer(query, " ");
			String zipPref = buddy.getPreference("zipcode");
			if ((zipPref == null) && (st.countTokens() < 2)) {
				super.sendMessage(buddy, "ERROR:\n" + help());
			} else {
				String imcommand = st.nextToken();
				String zipcode = "";
				if (st.hasMoreElements()) {
					zipcode = ((String) st.nextElement()).trim();
				} else if (zipPref != null) {
					zipcode = zipPref;
				}
				String result = "Couldn't get provider list.";

----------------------------------------
    public void performService(AIMBuddy buddy, String query) {
        if(query.trim().toLowerCase().startsWith("weather")) {
            StringTokenizer st=new StringTokenizer(query, " ");
            String zipPref=buddy.getPreference("zipcode");
            if((zipPref == null) && (st.countTokens() < 2)) {
                super.sendMessage(buddy, "ERROR:\n"+help());
            } else {
                String imcommand=st.nextToken();
                String zipcode="";
                if(st.hasMoreElements()) {
                    zipcode=((String) st.nextElement()).trim();
                } else if(zipPref != null) {
                    zipcode=zipPref;
                }
                String result="Couldn't get weather information";

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805992
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 119 131
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 157
----------------------------------------
        if (query.toLowerCase().startsWith("adduser")) {
            // add a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to add
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to != null) {

----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805995
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 157
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 177
----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805993
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 119 131
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 177
----------------------------------------
        if (query.toLowerCase().startsWith("adduser")) {
            // add a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to add
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to != null) {

----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805997
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 177
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 197
----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805996
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 157
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 197
----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805994
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 119 131
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 197
----------------------------------------
        if (query.toLowerCase().startsWith("adduser")) {
            // add a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to add
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to != null) {

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805998
src/com/levelonelabs/aim/AIMGroup.java 147 151
src/com/levelonelabs/aimbot/modules/ReminderModule.java 315 319
----------------------------------------
        buddies=new ArrayList();
        NodeList list=fullStateElement.getElementsByTagName("buddy");
        for(int i=0; i < list.getLength(); i++) {
            Element buddyElem=(Element) list.item(i);
            String name=buddyElem.getAttribute("name");

----------------------------------------
        reminders = new HashMap();
        NodeList list = fullStateElement.getElementsByTagName("buddy");
        for (int i = 0; i < list.getLength(); i++) {
            Element buddyElem = (Element) list.item(i);
            String name = buddyElem.getAttribute("name");

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26805999
src/com/levelonelabs/aimbot/modules/BabelModule.java 257 266
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 119 128
----------------------------------------
        if(query.trim().toLowerCase().startsWith("babel")) {
            StringTokenizer st=new StringTokenizer(query, " ");

            //check for enough args
            if(st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n"+help());
                return;
            }

            String imcommand=st.nextToken();

----------------------------------------
        if (query.toLowerCase().startsWith("adduser")) {
            // add a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to add
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806000
src/com/levelonelabs/aimbot/modules/BabelModule.java 257 266
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 154
----------------------------------------
        if(query.trim().toLowerCase().startsWith("babel")) {
            StringTokenizer st=new StringTokenizer(query, " ");

            //check for enough args
            if(st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n"+help());
                return;
            }

            String imcommand=st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806003
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 119 128
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 154
----------------------------------------
        if (query.toLowerCase().startsWith("adduser")) {
            // add a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to add
            String imcommand = st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806001
src/com/levelonelabs/aimbot/modules/BabelModule.java 257 266
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 174
----------------------------------------
        if(query.trim().toLowerCase().startsWith("babel")) {
            StringTokenizer st=new StringTokenizer(query, " ");

            //check for enough args
            if(st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n"+help());
                return;
            }

            String imcommand=st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806006
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 154
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 174
----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806004
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 119 128
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 174
----------------------------------------
        if (query.toLowerCase().startsWith("adduser")) {
            // add a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to add
            String imcommand = st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806008
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 174
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 194
----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806005
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 119 128
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 194
----------------------------------------
        if (query.toLowerCase().startsWith("adduser")) {
            // add a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to add
            String imcommand = st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806007
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 154
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 194
----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806002
src/com/levelonelabs/aimbot/modules/BabelModule.java 257 266
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 194
----------------------------------------
        if(query.trim().toLowerCase().startsWith("babel")) {
            StringTokenizer st=new StringTokenizer(query, " ");

            //check for enough args
            if(st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n"+help());
                return;
            }

            String imcommand=st.nextToken();

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806009
src/com/levelonelabs/aimbot/modules/MoviesModule.java 334 347
src/com/levelonelabs/aimbot/modules/MoviesModule.java 356 369
----------------------------------------
                (temp.toUpperCase().indexOf(" ]") == -1) &&
                (temp.toUpperCase().indexOf(" |") == -1) &&
                (temp.toUpperCase().indexOf("THEATER INFO") == -1) &&
                (temp.toUpperCase().indexOf("MAP IT") == -1)) {
            if(temp.toUpperCase().indexOf("BUY TICKETS") != -1) {
                /* Some theaters sell ticket online so each time is a link
                 * instead of one long string. Flag is set to create the
                 * one string.
                 */
                areLinkTimes=true;
            } else {
                if((line.length()+data.length) > 950) {
                    result.add(line);
                    line="<B>"+temp+"</B>";

----------------------------------------
                (temp.toUpperCase().indexOf("CLICK") == -1) &&
                (temp.toUpperCase().indexOf(" |") == -1) &&
                (temp.toUpperCase().indexOf("THEATER INFO") == -1) &&
                (temp.toUpperCase().indexOf("MAP IT") == -1)) {
            if(temp.toUpperCase().indexOf("BUY TICKETS") != -1) {
                /* Some theaters sell ticket online so each time is a link
                 * instead of one long string. Flag is set to create the
                 * one string.
                 */
                areLinkTimes=true;
            } else {
                if((line.length()+data.length) > 950) {
                    result.add(line);
                    line=temp;

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806010
src/com/levelonelabs/aimbot/modules/MoviesModule.java 386 395
src/com/levelonelabs/aimbot/modules/MoviesModule.java 475 484
----------------------------------------
    public ArrayList getTimes() {
        try {
            URL url=new URL(urlPart+zipcode);
            logger.fine("Looking for movies for: "+zipcode+" using URL="+url);
            URLConnection conn=url.openConnection();
            BufferedReader br=new BufferedReader(new InputStreamReader(conn.getInputStream()));
            ParserDelegator pd=new ParserDelegator();
            pd.parse(br, this, true);
        } catch(Exception e) {
            e.printStackTrace();

----------------------------------------
    public String getTheaters() {
        try {
            URL url=new URL(urlPart+zipcode);
            logger.fine("Looking for theaters for: "+zipcode+" using URL="+url);
            URLConnection conn=url.openConnection();
            BufferedReader br=new BufferedReader(new InputStreamReader(conn.getInputStream()));
            ParserDelegator pd=new ParserDelegator();
            pd.parse(br, this, true);
        } catch(Exception e) {
            e.printStackTrace();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806011
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 119 131
src/com/levelonelabs/aimbot/modules/MessengerModule.java 270 283
----------------------------------------
        if (query.toLowerCase().startsWith("adduser")) {
            // add a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to add
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to != null) {

----------------------------------------
		if (query.toLowerCase().startsWith("offline")) {
			StringTokenizer st = new StringTokenizer(query, " ");

			//check for proper params
			if (st.countTokens() < 3) {
				super.sendMessage(buddy, "ERROR:\n" + help());
				return;
			}

			//grab the target
			String imcommand = st.nextToken();
			String imcommandTo = st.nextToken();
			AIMBuddy to = super.getBuddy(imcommandTo);
			if (to == null) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806012
src/com/levelonelabs/aimbot/modules/BabelModule.java 257 266
src/com/levelonelabs/aimbot/modules/MessengerModule.java 270 280
----------------------------------------
        if(query.trim().toLowerCase().startsWith("babel")) {
            StringTokenizer st=new StringTokenizer(query, " ");

            //check for enough args
            if(st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n"+help());
                return;
            }

            String imcommand=st.nextToken();

----------------------------------------
		if (query.toLowerCase().startsWith("offline")) {
			StringTokenizer st = new StringTokenizer(query, " ");

			//check for proper params
			if (st.countTokens() < 3) {
				super.sendMessage(buddy, "ERROR:\n" + help());
				return;
			}

			//grab the target
			String imcommand = st.nextToken();

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806013
src/com/levelonelabs/aim/AIMClient.java 909 916
src/com/levelonelabs/aim/AIMClient.java 935 942
----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleError(error, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
            for (int i = 0; i < aimListeners.size(); i++) {
                try {
                    ((AIMListener) aimListeners.get(i)).handleBuddySignOn(aimbud, message);
                } catch (Exception e) {
                    e.printStackTrace();
                }
            }
        }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806014
src/com/levelonelabs/aim/AIMClient.java 909 916
src/com/levelonelabs/aim/AIMClient.java 962 969
----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleError(error, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddySignOff(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806017
src/com/levelonelabs/aim/AIMClient.java 935 942
src/com/levelonelabs/aim/AIMClient.java 962 969
----------------------------------------
            for (int i = 0; i < aimListeners.size(); i++) {
                try {
                    ((AIMListener) aimListeners.get(i)).handleBuddySignOn(aimbud, message);
                } catch (Exception e) {
                    e.printStackTrace();
                }
            }
        }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddySignOff(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806015
src/com/levelonelabs/aim/AIMClient.java 909 916
src/com/levelonelabs/aim/AIMClient.java 986 993
----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleError(error, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddyAvailable(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806020
src/com/levelonelabs/aim/AIMClient.java 962 969
src/com/levelonelabs/aim/AIMClient.java 986 993
----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddySignOff(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddyAvailable(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806018
src/com/levelonelabs/aim/AIMClient.java 935 942
src/com/levelonelabs/aim/AIMClient.java 986 993
----------------------------------------
            for (int i = 0; i < aimListeners.size(); i++) {
                try {
                    ((AIMListener) aimListeners.get(i)).handleBuddySignOn(aimbud, message);
                } catch (Exception e) {
                    e.printStackTrace();
                }
            }
        }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddyAvailable(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806021
src/com/levelonelabs/aim/AIMClient.java 962 969
src/com/levelonelabs/aim/AIMClient.java 1011 1018
----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddySignOff(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddyUnavailable(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806022
src/com/levelonelabs/aim/AIMClient.java 986 993
src/com/levelonelabs/aim/AIMClient.java 1011 1018
----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddyAvailable(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddyUnavailable(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806019
src/com/levelonelabs/aim/AIMClient.java 935 942
src/com/levelonelabs/aim/AIMClient.java 1011 1018
----------------------------------------
            for (int i = 0; i < aimListeners.size(); i++) {
                try {
                    ((AIMListener) aimListeners.get(i)).handleBuddySignOn(aimbud, message);
                } catch (Exception e) {
                    e.printStackTrace();
                }
            }
        }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddyUnavailable(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806016
src/com/levelonelabs/aim/AIMClient.java 909 916
src/com/levelonelabs/aim/AIMClient.java 1011 1018
----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleError(error, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
        for (int i = 0; i < aimListeners.size(); i++) {
            try {
                ((AIMListener) aimListeners.get(i)).handleBuddyUnavailable(aimbud, message);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806023
src/com/levelonelabs/aimbot/modules/MessengerModule.java 270 285
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 158
----------------------------------------
		if (query.toLowerCase().startsWith("offline")) {
			StringTokenizer st = new StringTokenizer(query, " ");

			//check for proper params
			if (st.countTokens() < 3) {
				super.sendMessage(buddy, "ERROR:\n" + help());
				return;
			}

			//grab the target
			String imcommand = st.nextToken();
			String imcommandTo = st.nextToken();
			AIMBuddy to = super.getBuddy(imcommandTo);
			if (to == null) {
				super.sendMessage(buddy, "User " + imcommandTo
					+ " does not exist in the system.  Use the ADDUSER command to add them.");

----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806026
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 158
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 178
----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806024
src/com/levelonelabs/aimbot/modules/MessengerModule.java 270 285
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 178
----------------------------------------
		if (query.toLowerCase().startsWith("offline")) {
			StringTokenizer st = new StringTokenizer(query, " ");

			//check for proper params
			if (st.countTokens() < 3) {
				super.sendMessage(buddy, "ERROR:\n" + help());
				return;
			}

			//grab the target
			String imcommand = st.nextToken();
			String imcommandTo = st.nextToken();
			AIMBuddy to = super.getBuddy(imcommandTo);
			if (to == null) {
				super.sendMessage(buddy, "User " + imcommandTo
					+ " does not exist in the system.  Use the ADDUSER command to add them.");

----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806028
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 165 178
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 198
----------------------------------------
        } else if (query.toLowerCase().startsWith("denyuser")) {
            // deny a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to deny
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806027
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 145 158
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 198
----------------------------------------
        } else if (query.toLowerCase().startsWith("rmuser")) {
            // remove a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to remove
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806025
src/com/levelonelabs/aimbot/modules/MessengerModule.java 270 285
src/com/levelonelabs/aimbot/modules/UserAdminModule.java 185 198
----------------------------------------
		if (query.toLowerCase().startsWith("offline")) {
			StringTokenizer st = new StringTokenizer(query, " ");

			//check for proper params
			if (st.countTokens() < 3) {
				super.sendMessage(buddy, "ERROR:\n" + help());
				return;
			}

			//grab the target
			String imcommand = st.nextToken();
			String imcommandTo = st.nextToken();
			AIMBuddy to = super.getBuddy(imcommandTo);
			if (to == null) {
				super.sendMessage(buddy, "User " + imcommandTo
					+ " does not exist in the system.  Use the ADDUSER command to add them.");

----------------------------------------
        } else if (query.toLowerCase().startsWith("permituser")) {
            // permit a user
            StringTokenizer st = new StringTokenizer(query, " ");
            if (st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n" + help());
                return;
            }

            // grab the user to permit
            String imcommand = st.nextToken();
            String imcommandTo = st.nextToken();
            AIMBuddy to = super.getBuddy(imcommandTo);
            if (to == null) {
                super.sendMessage(buddy, "User " + imcommandTo + " does not exist in the system.");

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806029
src/com/levelonelabs/aimbot/modules/MoviesModule.java 282 284
src/com/levelonelabs/aimbot/modules/TVListingsModule.java 245 247
----------------------------------------
    public void handleStartTag(HTML.Tag t, MutableAttributeSet a, int pos) {
        if((t == HTML.Tag.A) && (a.getAttribute(HTML.Attribute.NAME) != null) &&
                a.getAttribute(HTML.Attribute.NAME).equals("T"+theater)) {

----------------------------------------
	public void handleStartTag(HTML.Tag t, MutableAttributeSet a, int pos) {
		if ((t == HTML.Tag.SELECT) && (a.getAttribute(HTML.Attribute.NAME) != null)
			&& a.getAttribute(HTML.Attribute.NAME).equals("lineup")) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806030
src/com/levelonelabs/aimbot/modules/MoviesModule.java 480 485
src/com/levelonelabs/aimbot/modules/TVListingsModule.java 622 628
----------------------------------------
            BufferedReader br=new BufferedReader(new InputStreamReader(conn.getInputStream()));
            ParserDelegator pd=new ParserDelegator();
            pd.parse(br, this, true);
        } catch(Exception e) {
            e.printStackTrace();
            return result;

----------------------------------------
			BufferedReader br = new BufferedReader(new InputStreamReader(conn.getInputStream()));

			ParserDelegator pd = new ParserDelegator();
			pd.parse(br, this, true);
		} catch (Exception e) {
			e.printStackTrace();
			return null;

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806031
src/com/levelonelabs/aimbot/modules/SkiReportModule.java 302 316
src/com/levelonelabs/aimbot/modules/TVListingsModule.java 301 315
----------------------------------------
    public Map getMountains() {
        HashMap result = new HashMap();
        try {
            URL url = new URL(BASE_URL + this.state + "/index.html");
            logger.fine("Looking for mountains in:" + state + " using URL= " + url);
            URLConnection conn = url.openConnection();
            BufferedReader br = new BufferedReader(new InputStreamReader(conn.getInputStream()));
            ParserDelegator pd = new ParserDelegator();
            pd.parse(br, this, true);
            if (codes.size() == names.size()) {
                for (int i = 0; i < codes.size(); i++) {
                    result.put(codes.get(i), names.get(i));
                }
            }
        } catch (Exception e) {

----------------------------------------
	public Map getProviders() {
		HashMap result = new HashMap();
		try {
			URL url = new URL(BASE_URL + this.zipcode);
			logger.fine("Looking for prividers for:" + zipcode + " using URL= " + url);
			URLConnection conn = url.openConnection();
			BufferedReader br = new BufferedReader(new InputStreamReader(conn.getInputStream()));
			ParserDelegator pd = new ParserDelegator();
			pd.parse(br, this, true);
			if (codes.size() == names.size()) {
				for (int i = 0; i < codes.size(); i++) {
					result.put(codes.get(i), names.get(i));
				}
			}
		} catch (Exception e) {

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806032
src/com/levelonelabs/aimbot/modules/SkiReportModule.java 334 339
src/com/levelonelabs/aimbot/modules/TVListingsModule.java 333 338
----------------------------------------
        for (Iterator iter = mounts.keySet().iterator(); iter.hasNext();) {
            String key = (String) iter.next();
            String value = (String) mounts.get(key);
            System.out.println(value + "\t use --> " + key);
        }
    }

----------------------------------------
		for (Iterator iter = providers.keySet().iterator(); iter.hasNext();) {
			String key = (String) iter.next();
			String value = (String) providers.get(key);
			System.out.println(value + "\t use --> " + key);
		}
	}

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806033
src/com/levelonelabs/aimbot/modules/SkiReportModule.java 489 501
src/com/levelonelabs/aimbot/modules/TVListingsModule.java 447 459
----------------------------------------
            URLConnection conn = url.openConnection();
            BufferedReader br = new BufferedReader(new InputStreamReader(conn.getInputStream()));
            ParserDelegator pd = new ParserDelegator();
            pd.parse(br, this, true);

            tempResult = result;
            if (statesVisited != 6) {
                tempResult = null;
            }
        } catch (Exception e) {
            // e.printStackTrace();
            logger.severe(e.toString());
            return null;

----------------------------------------
			URLConnection conn = this.url.openConnection();
			BufferedReader br = new BufferedReader(new InputStreamReader(conn.getInputStream()));
			ParserDelegator pd = new ParserDelegator();
			pd.parse(br, this, true);

			tempResult = getResult();
			if (!foundStart || !foundEnd) {
				tempResult = "";
			}
		} catch (Exception e) {
			//e.printStackTrace();
			logger.severe(e.toString());
			return "";

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806034
src/com/levelonelabs/aim/example/AIMReceptionist.java 89 96
src/com/levelonelabs/aimbot/modules/MessengerModule.java 157 164
----------------------------------------
				if (buddy.getName().equalsIgnoreCase(newName.getName()) && buddy.hasMessages()) {
					ArrayList messages = buddy.getMessages();
					String message = "";

					//collect the messages
					for (int i = 0; i < messages.size(); i++) {
						message += (messages.get(i) + "<BR>");
					}

----------------------------------------
		if (buddy.hasMessages()) {
			ArrayList messages = buddy.getMessages();
			String message = "";

			//collect the messages
			for (int i = 0; i < messages.size(); i++) {
				message += (messages.get(i) + "<BR>");
			}

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806035
src/com/levelonelabs/aimbot/modules/BuddyManagementModule.java 153 168
src/com/levelonelabs/aimbot/modules/BuddyManagementModule.java 172 188
----------------------------------------
            if (option.equalsIgnoreCase("buddies")) {
                int numRemoved = 0;
                Iterator iter = super.getBuddyNames();
                List toRemoveList = new ArrayList();
                while (iter.hasNext()) {
                    String name = (String) iter.next();
                    AIMBuddy bud = super.getBuddy(name);
                    // if we found a buddy without user/admin role, that isnt
                    // us
                    if (!bud.hasRole(AIMBot.ROLE_USER) && !bud.hasRole(AIMBot.ROLE_ADMINISTRATOR)
                        && !super.bot.getUsername().equalsIgnoreCase(name)) {
                        logger.fine("Removing non-user buddy " + bud.getName());
                        toRemoveList.add(bud);
                        numRemoved++;
                    }
                }

----------------------------------------
            } else if (option.equalsIgnoreCase("inactive")) {
                int numRemoved = 0;
                Iterator iter = super.getBuddyNames();
                List toRemoveList = new ArrayList();
                while (iter.hasNext()) {
                    String name = (String) iter.next();
                    AIMBuddy bud = super.getBuddy(name);
                    // if we found a user without admin role, that isnt us
                    if (bud.hasRole(AIMBot.ROLE_USER) && !bud.hasRole(AIMBot.ROLE_ADMINISTRATOR)
                        && !super.bot.getUsername().equalsIgnoreCase(name)) {
                        // if they are inactive, remove them
                        if (isInactive(bud)) {
                            logger.fine("Removing inactive buddy " + bud.getName());
                            toRemoveList.add(bud);
                            numRemoved++;
                        }
                    }

----------------------------------------
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
97
26806036
src/com/levelonelabs/aimbot/modules/DefaultModule.java 117 125
src/com/levelonelabs/aimbot/modules/MegaHalModule.java 174 185
----------------------------------------
        if(query.toLowerCase().startsWith("addTerm")) {
            if(!buddy.hasRole(AIMBot.ROLE_ADMINISTRATOR)) {
                super.sendMessage(buddy, "Sorry, only admins can add terms");
                return;
            }
            if(st.countTokens() < 2) {
                super.sendMessage(buddy, "ERROR:\n"+help());
                return;
            }

----------------------------------------
		if (query.toLowerCase().startsWith("megahaladmin")) {
			if (!buddy.hasRole(AIMBot.ROLE_ADMINISTRATOR)) {
				super.sendMessage(buddy, "Sorry, you are not an " + AIMBot.ROLE_ADMINISTRATOR);
				return;
			}
			StringTokenizer st = new StringTokenizer(query, " ");

			//check for enough args
			if (st.countTokens() < 2) {
				super.sendMessage(buddy, "ERROR:\n" + help());
				return;
			}

----------------------------------------
